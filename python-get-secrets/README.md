# Python to get aws secrets manager secret_string

This is just a test written by python to get the password from aws secrets manager


## Requirements

- boto3 need to be installed to call aws api

- the machine which run this code needs to have IAM role for aws secrets manager and kms_key decrypt access


## Usage

```shell

python3 get_secrets.py

```

